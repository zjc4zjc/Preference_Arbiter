 # 🧠 Method: [方法名称]

 > **Agent Instruction:** 本文件用于以「数学为中心」描述与分析本研究的方法。  
 > 填写时优先追求：**数学先行指导设计 → 方法中有清晰定理/推论 → 证明与实验形成闭环**。

 ---

 ## 1. 方法总览（High-Level Overview）

 - **一句话方法总结：**  
   > 例：我们提出一种基于 \([核心思想]\) 的方法，通过优化 \([目标]\) 来提升 \([指标]\)。
 - **核心直觉（Intuition）：**  
   从非形式角度解释：为什么这个方法“看起来应该有效”？  
 - **数学层级定位（Optional）：**  
   - ✅ **目标层级（最优）：** 先有数学目标/定理，再据此设计算法。  
   - ⚖️ **解释层级（中等）：** 算法先于理论，但可以用数学模型清晰解释其奏效原因。  
   - 📌 **描述层级（基础）：** 算法可以被形式化描述，但暂时没有严格推导或证明。

 ---

 ## 2. 记号与问题设定（Notation & Problem Setup）

 ### 2.1 记号约定（Notation）

 ### 2.2 问题形式化（Problem Formalization）

 - **任务目标：**  
   用形式化方式定义我们要优化的对象，
   
 - **证明思路（Proof Sketch）：**
   1. 第一步：将目标转化为某种界（upper/lower bound）；
   2. 第二步：利用不等式/信息论/概率工具（例如 Jensen、不等式、PAC-Bayes 等）；
   3. 第三步：比较我们的方法与 Baseline 的关键差异点；
   4. 得出我们方法在某指标上更优/不劣的结论。

 ### 4.2 推论与特例（Corollaries & Special Cases）

 - **Corollary 1（特定场景下的简化）：**  
   在假设 A1–A3 且额外满足 \([简化条件]\) 时，定理 1 可简化为：
   \[
   \mathcal{M}(f_\theta) \geq \mathcal{M}(f_{\theta_{\text{baseline}}}) + \Delta
   \]
   其中 \(\Delta > 0\) 可被显式下界。

 - **Special Case：**  
   当 \([参数设定/噪声分布]\) 取某值时，我们的方法退化为已知方法 \([方法名]\)，这为我们提供与文献的数学对齐。

 ---

 ## 5. 理论解释：为什么方法奏效？（Why It Works）

 从「中等层级」的要求出发，在此部分给出：

 - **机制解释：**  
   - 我们的正则/约束项在数学上如何改变解空间？  
   - 它避免了哪些已知失败模式（如过拟合、梯度消失、对抗脆弱性）？
 - **对比分析：**  
   - 与 Baseline 的目标函数或约束的差别，能否形式化地说明“我们更偏好某类好解”？  
 - **几何/信息论视角（如适用）：**  
   例如：
   - 在表示空间中拉开类间距离、压缩类内距离；
   - 减少互信息 \( I(\text{噪声}; \text{决策}) \)；
   - 增强马尔可夫链中某些依赖路径的权重。

 ---

 ## 6. 与实验设计的对齐（Link to Experiments）

 - **可验证的理论结论：**  
   列出可以在实验中直接检验的数学结论，例如：
   - 某个界的单调性（随参数变化的趋势）；
   - 某个稳定性指标与最终性能的相关性；
 - **实验指标与数学量的对应表：**
   - 理论中的 \( \mathcal{M} \) ↔ 实验中的指标（例如 Accuracy / Robust Accuracy / Expected Reward 等）；
   - 理论中的正则项 ↔ 可观测的统计量（如范数大小、梯度方差、表示分布距离）。

 ---


 ## 7. Agent 追踪备注（Agent Notes）

 > **本节由 Agent 更新，用于记录方法/公式的演化历程。**

 - **[YYYY-MM-DD] 数学结构更新：**  
   示例：调整了总损失中的权重 \(\alpha, \beta, \gamma\)，因为实验发现鲁棒性不足，需要加强 \(\mathcal{L}_{\text{defense}}\) 的权重。
 - **当前数学瓶颈：**  
   - 例如：定理 1 中某步不等式过于粗糙，希望找到更紧的工具；  
   - 或：现有证明依赖强假设 A2，后续尝试放宽。

